#ifndef FREE_H
#define FREE_H

// Includes.
#include "wizard.h"

// Prototypes for the public functions of the module.
int jsonDelete(node_t **, char *);
int jsonQuit(node_t *, char **);
int freeData(node_t *);

#endif
